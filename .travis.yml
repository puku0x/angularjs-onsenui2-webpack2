sudo: false

language: node_js
node_js:
  - "6"

cache:
  directories:
    - node_modules
    - $HOME/.yarn-cache

before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash
  - export PATH="$HOME/.yarn/bin:$PATH"

install:
  - yarn install

before_script:

script:
  - yarn build
